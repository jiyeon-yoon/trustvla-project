[project]
name = "trustvla"
version = "0.1.0"
description = "Paired instruction and safety evaluation scaffold for VLA policies."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
trustvla = "trustvla.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

